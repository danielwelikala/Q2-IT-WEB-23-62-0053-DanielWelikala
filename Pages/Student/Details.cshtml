@page
@model Q2.Pages.Student.DetailsModel

@{
    ViewData["Title"] = "Student Details";
}

<h1>Student Details</h1>

<div>
    <h4>Students</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Students.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Students.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Students.City)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Students.City)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Students?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
